# mapIsEmpty
> Fonctions

Cette fonction est disponible à partir du LeekScript 4 et plus.
Renvoie `true` si la table est vide, `false` si elle contient au moins une (clé, valeur).

#### Paramètres

- **map** : La table

#### Retour

- **isEmpty** : `true` si la table est vide, `false` sinon.
